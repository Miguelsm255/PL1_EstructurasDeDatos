//Pila donde se almacenan los datos por prioridades
Al inicio, la pila está vacía, se van añadiendo elementos.

añadir_elemento nuestro_elemento, cola -> cola
        c_aux <- c_vacia
        si (es_vacia? cola) entonces apilar: nuestro_elemento 
        sino:
                mientras((primero:cola).prioridad >= nuestro_elemento.prioridad))
                        e <- primero(cola)
                        eliminar (cola)
                        añadir(e, c_aux)
                
                finmientras
                añadir(nuestro_elemento, c_aux)
                mientras !(es_vacia?cola)
                añadir (primero(cola), c_aux)
                finmientras
        finsi
        ffun



elemento_a_nucleo pila, cola -> 


